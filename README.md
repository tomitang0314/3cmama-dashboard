# 3CMAMA 家長手機頁

靜態網頁，給 [3CMAMA](https://github.com) 螢幕時間管控 app 的家長用。API 在 Supabase Edge Functions；這裡只有畫面，沒有任何秘密——沒有家庭金鑰的人打開只會看到登入頁。

原始碼在主專案的 `web/dashboard.html`；更新時把它複製成這裡的 `index.html` 推上 main 即可。
